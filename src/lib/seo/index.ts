/**
 * SEO toolkit — single import point.
 *
 * Usage:
 *   import { buildMetadata, organizationSchema, COMPANY } from '@/lib/seo'
 */

export * from './business-data'
export * from './metadata'
export * from './schema'
